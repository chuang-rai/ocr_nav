# OCRNav: Using OCR for Localization and Navigation

## Setup
1. Install conda and run the following command to create the conda environment
```bash
conda env create -f environment.yaml
conda activate ocr_nav
pip install flash-attn==2.7.3 --no-build-isolation
pip install -e .[dev]
```




